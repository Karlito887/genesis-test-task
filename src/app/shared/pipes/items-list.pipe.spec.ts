import { ItemsListPipe } from './items-list.pipe';

describe('ItemsListPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemsListPipe();
    expect(pipe).toBeTruthy();
  });
});
